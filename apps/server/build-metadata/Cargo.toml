[package]
name = "build-metadata"
edition.workspace = true
version.workspace = true


build = "build.rs"

[lib]
path = "mod.rs"
crate-type = [
  "rlib",
  # "dylib",
]

[features]


[dependencies]

[build-dependencies]
built = { workspace = true }

[lints]
