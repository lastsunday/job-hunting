[package]
name = "job-hunting-server"
version = "0.1.0"
edition = "2021"
publish = false

[profile.release]
opt-level = 3
lto = true
codegen-units = 8
strip = true
debug = false

[dependencies]
api = { path = "api" }
