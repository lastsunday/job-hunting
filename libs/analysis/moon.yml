language: 'typescript'
layer: 'library'
tags: ['lit', 'web-components']

tasks:
  build:
    command: "pnpm"
    args: ["--filter", "analysis", "run", "build"]
    inputs:
      - '**/*'
      - '@group(js-shared)'
    deps:
      - ~:ensure-deps
